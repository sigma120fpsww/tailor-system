# Добавление функции скидки
        has_card = input("Есть карта постоянного клиента? (да/нет): ").lower()
        if has_card == "да":
            total *= 0.9
            print(f"Цена со скидкой 10%: {total} руб.")
